import React from 'react';
import css from './Heading.css';

// const classes = classNames(
//   border.simple,
//   padding.standard,
// );

export const Container = ({children}) => <div className={css.default}>{children}</div>;
